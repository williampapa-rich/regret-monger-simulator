# 껄무새 시뮬레이터 (Regret-monger Simulator)

> "그때 그거 살 걸..."

## 목차

- [1. 프로그램 소개](#1-프로그램-소개)
  - [1.1 취지](#11-취지)
  - [1.2 핵심 기능](#12-핵심-기능)
- [2. 사용 방법](#2-사용-방법)
  - [2.1 일시매수 시나리오](#21-일시매수-시나리오)
  - [2.2 DCA(분할매수) 시나리오](#22-dca분할매수-시나리오)
  - [2.3 벤치마크 비교](#23-벤치마크-비교)
  - [2.4 포트폴리오 저장/불러오기](#24-포트폴리오-저장불러오기)
- [3. 변수 / 입력값 설명](#3-변수--입력값-설명)
  - [3.1 마켓 (Market)](#31-마켓-market)
  - [3.2 매수가 기준 (Price Basis)](#32-매수가-기준-price-basis)
  - [3.3 입력 방식 (Amount Type)](#33-입력-방식-amount-type)
  - [3.4 수수료율 (Fee Rate)](#34-수수료율-fee-rate)
  - [3.5 수익률 정의](#35-수익률-정의)
- [4. 설치 — 비개발자용 따라하기](#4-설치--비개발자용-따라하기)
  - [4.1 미리 알아두세요](#41-미리-알아두세요)
  - [4.2 macOS 설치](#42-macos-설치)
  - [4.3 Windows 설치](#43-windows-설치)
  - [4.4 KRX 계정 만들고 .env 채우기 (한국 주식용)](#44-krx-계정-만들고-env-채우기-한국-주식용)
  - [4.5 매번 실행하는 방법](#45-매번-실행하는-방법)
  - [4.6 자주 막히는 곳 / 문제 해결](#46-자주-막히는-곳--문제-해결)
- [5. 시간대·정수매매 등 주의사항](#5-시간대정수매매-등-주의사항)
- [6. v1 범위 외 (TODO)](#6-v1-범위-외-todo)

---

## 1. 프로그램 소개

### 1.1 취지

특정 자산을 과거의 어느 시점에 매수했다면 지금은 어떻게 됐을지를 계산해주는
macOS / Windows 데스크톱 앱이다. "재무모델링 II" 학기 프로젝트로, PyCharm + PyQt + REST API를
실습하기 위한 목적도 겸한다.

다음 질문들에 즉답한다.
- 2020년 1월 2일에 삼성전자에 100만원을 넣었다면 지금 평가액은?
- 매월 1일 BTC에 100 USDT씩 1년간 적립식으로 샀다면?
- 같은 금액으로 KOSPI200 ETF를 샀을 때와 비교하면?

### 1.2 핵심 기능

- **일시매수 / DCA(분할매수) 시뮬레이션**: 단일 시점 매수, 그리고 일·주·월 단위 적립식 매수
- **차트 시각화**: PyQtGraph 캔들스틱 + 매수 시점 마커 + 호버 시 십자선/OHLC 라벨
- **차트 클릭 입력**: 캔들을 클릭하면 매수일이 자동 입력
- **벤치마크 비교**: 같은 금액으로 다른 자산을 샀을 때와 수익률 차이 (동일 통화 내)
- **자동완성 종목 검색**: 마켓별 종목 디렉터리를 받아 "삼"만 쳐도 "삼성전자" 자동 매칭
- **포트폴리오 영속성**: JSON으로 저장/불러오기
- **부가 요소**: "껄무새" 마스코트가 매수 직후 살껄~ 외치다 블랙홀로 빨려들어가는 애니메이션

---

## 2. 사용 방법

### 2.1 일시매수 시나리오

1. **마켓** 선택: KOSPI / KOSDAQ / NASDAQ / NYSE / Binance / Upbit 중 하나
2. **종목** 입력: 자동완성 — 한글/영문 substring 검색 (예: "삼성전" → "삼성전자")
3. **차트 시작/종료** 일자 지정 후 **차트 불러오기**
4. 차트가 뜨면 **캔들 클릭**으로 매수일 자동 선택, 또는 매수일 직접 입력
5. **매수가 기준** (시가/종가/평균) 선택
6. **입력 방식**: 수량 또는 금액
7. **값** 입력 (천단위 콤마 자동 삽입)
8. **매수 추가** → 평가 결과 테이블에 한 줄 추가, 마스코트가 살껄~ 외침

### 2.2 DCA(분할매수) 시나리오

1. 차트를 먼저 불러온 상태에서 **DCA(분할매수) 추가…** 버튼
2. 시작/종료, 주기(매일/매주/매월), 회차 금액 입력
3. 확인 → 모든 회차가 한 번에 추가됨
4. 휴장일은 자동으로 직후 영업일로 스냅 (수동 입력 시에는 직전/직후 선택 다이얼로그)

### 2.3 벤치마크 비교

1. 매수 내역이 한 건 이상 추가된 상태
2. 우측 **벤치마크 비교** 패널에서 마켓·종목 선택 → **비교**
3. 통화 불일치 시 비활성화 + "통화 불일치" 안내 (예: KRW 포트폴리오에 SPY 선택 불가)

### 2.4 포트폴리오 저장/불러오기

- 메뉴 → **파일 → 저장…** : 현재 매수 내역을 JSON으로 저장
- 메뉴 → **파일 → 불러오기…** : 저장된 JSON을 다시 불러옴

---

## 3. 변수 / 입력값 설명

### 3.1 마켓 (Market)

| 마켓 | 데이터 소스 | 통화 | 정수매매 | 티커 포맷 예시 |
|---|---|---|---|---|
| KOSPI | yfinance | KRW | ○ | `005930.KS` (삼성전자) |
| KOSDAQ | yfinance | KRW | ○ | `091990.KQ` (셀트리온헬스케어) |
| NASDAQ | yfinance | USD | ○ | `AAPL`, `^IXIC` |
| NYSE | yfinance | USD | ○ | `KO`, `^GSPC` |
| Binance | python-binance | USDT | ✕ (소수점) | `BTCUSDT`, `ETHUSDT` |
| Upbit | pyupbit | KRW | ✕ (소수점) | `KRW-BTC`, `KRW-ETH` |

### 3.2 매수가 기준 (Price Basis)

매수일 캔들의 어느 가격을 매수단가로 쓸지.

- **시가 (Open)**: 그날 첫 거래 가격
- **종가 (Close)**: 그날 마지막 가격
- **평균 (Average)**: `(시가 + 종가) / 2` — 단순 추정값

### 3.3 입력 방식 (Amount Type)

- **수량 (Quantity)**: "10주" 같이 보유 수량을 직접 지정
- **금액 (Amount)**: "100만원어치" 같이 투입 총액을 지정

정수매매 마켓에서는 금액 입력 시:
- `수량 = floor(금액 / (매수단가 × (1 + 수수료율)))`
- 남는 돈은 **현금잔액**으로 보유했다고 가정 (테이블에 별도 컬럼)

### 3.4 수수료율 (Fee Rate)

`regret/config/fees.toml`에 마켓별로 정의. 매수·매도 양쪽에 동일하게 적용.

| 마켓 | 기본값 | 비고 |
|---|---|---|
| Binance | 0.001 (0.1%) | Spot taker |
| Upbit | 0.0005 (0.05%) | KRW 마켓 |
| KOSPI/KOSDAQ | 0.00015 (0.015%) | 가정 증권사 기준 |
| NASDAQ/NYSE | 0.0 | 가정 증권사 기준 (실제 값은 증권사별 상이) |

각 매수 건은 **생성 시점의 수수료율을 박아둔다** — 정책이 바뀌어도 과거 시뮬레이션은 그대로 재현된다.

### 3.5 수익률 정의

- **gross_return_pct**: 가격 변동만 반영. `(현재가 / 매수단가 - 1) × 100`
- **net_return_pct**: 수수료 + 정수매매 잔돈을 모두 반영한 실제 손익률.
  `(평가액 / 투입금액 - 1) × 100`
  - 평가액 = `보유 수량 × 현재가 × (1 - 수수료율)` + 현금잔액 (정수매매에서 발생)

---

## 4. 설치 — 비개발자용 따라하기

> 코딩 안 해보신 분도 따라할 수 있게 적었습니다.
> **순서대로 복사·붙여넣기**만 하시면 돼요. 막히면 [4.6 자주 막히는 곳](#46-자주-막히는-곳--문제-해결)으로 가세요.

### 4.1 미리 알아두세요

- 이 앱은 **Python 3.11 이상**이 필요합니다.
- 인터넷이 연결돼 있어야 첫 실행 시 시세 데이터를 받습니다.
- 설치하다가 비밀번호를 묻는 창이 뜨면 본인 컴퓨터 비밀번호를 입력하시면 됩니다.

전체 흐름은 이렇습니다:
1. 압축파일 풀기
2. Python 설치 (이미 있으면 건너뛰기)
3. 터미널 열기 → 명령 4줄 복사·붙여넣기
4. KRX 계정 만들고 `.env` 파일에 적기
5. 앱 실행

---

### 4.2 macOS 설치

#### ① 압축 풀기

받으신 `Regret-monger-simulator.zip`을 **더블클릭**하세요. 같은 위치에
`Regret-monger-simulator` 폴더가 생깁니다. 이 폴더를 **Documents**나 **Desktop** 같이
찾기 쉬운 곳으로 옮겨두세요.

#### ② Python 설치 확인

먼저 설치돼 있는지 확인합니다.

1. `command` + `space` → "터미널" 검색해서 실행
2. 터미널에 아래 명령을 붙여넣고 `Enter`:
   ```bash
   python3 --version
   ```
3. `Python 3.11.x` 또는 `Python 3.12.x` 또는 `Python 3.13.x`라고 나오면 ✅ **다음 단계로**.
   - `command not found` 또는 `Python 3.10.x` 이하가 나오면 ⬇️ **설치 필요**.

**Python이 없거나 버전이 낮은 경우**:
- <https://www.python.org/downloads/macos/> 접속
- 페이지 상단의 **노란색 "Download Python 3.13.x"** 버튼 클릭
- 받아진 `.pkg` 파일 더블클릭 → 안내대로 "계속 → 설치" (약 3분)
- 설치 끝난 후 **터미널을 한 번 닫았다가 다시** 열기

#### ③ 프로젝트 폴더로 이동

터미널에 다음을 입력합니다 (폴더를 옮긴 위치에 맞춰서):

```bash
cd ~/Desktop/Regret-monger-simulator
```

> Documents에 두셨다면 `cd ~/Documents/Regret-monger-simulator`

**팁**: `cd ` 까지만 치고 폴더를 터미널 창에 **드래그&드롭**하면 경로가 자동으로 입력됩니다.

#### ④ 설치 명령 (한 번만 하면 됩니다)

아래 한 덩어리를 통째로 복사해서 터미널에 붙여넣고 `Enter`:

```bash
python3 -m venv .venv
.venv/bin/pip install --upgrade pip
.venv/bin/pip install -e ".[dev]"
```

설치에 **3~5분** 정도 걸립니다. 빨간 글씨가 잠깐 보여도 마지막에 `Successfully installed`가
나오면 성공입니다.

이제 [4.4 KRX 계정 만들기](#44-krx-계정-만들고-env-채우기-한국-주식용)로 넘어가세요.

---

### 4.3 Windows 설치

#### ① 압축 풀기

받으신 `Regret-monger-simulator.zip`을 **마우스 오른쪽 클릭 → "압축 풀기"**.
같은 위치에 `Regret-monger-simulator` 폴더가 생깁니다.
**바탕 화면**이나 **문서** 폴더로 옮겨두세요.

#### ② Python 설치 확인

1. `Win` + `R` → `powershell` 입력 후 `Enter` (PowerShell 창이 열립니다)
2. 다음을 붙여넣고 `Enter`:
   ```powershell
   python --version
   ```
3. `Python 3.11.x` 이상이면 ✅ **다음 단계로**.
   - 그 외 결과(설치 안내가 뜨거나 3.10 이하)면 ⬇️ **설치 필요**.

**Python이 없거나 버전이 낮은 경우**:
- <https://www.python.org/downloads/windows/> 접속
- **노란색 "Download Python 3.13.x"** 버튼 클릭
- 받아진 `.exe` 파일 더블클릭
- ⚠️ **반드시 첫 화면에서 "Add python.exe to PATH" 체크박스를 켜세요!**
  (안 켜면 명령이 안 먹힙니다)
- "Install Now" → 끝나면 PowerShell 창을 한 번 **닫았다가 다시** 열기

#### ③ 프로젝트 폴더로 이동

PowerShell에 다음을 입력 (폴더를 둔 위치에 맞춰서):

```powershell
cd $HOME\Desktop\Regret-monger-simulator
```

> 문서 폴더에 두셨다면 `cd $HOME\Documents\Regret-monger-simulator`

**팁**: 파일 탐색기에서 폴더를 연 뒤 주소창을 클릭하면 전체 경로가 보입니다.
그걸 복사해서 `cd ` 다음에 붙여넣어도 됩니다.

#### ④ 설치 명령 (한 번만 하면 됩니다)

아래 한 덩어리를 통째로 복사해서 PowerShell에 붙여넣고 `Enter`:

```powershell
python -m venv .venv
.\.venv\Scripts\pip install --upgrade pip
.\.venv\Scripts\pip install -e ".[dev]"
```

설치에 **3~5분** 정도 걸립니다. 마지막에 `Successfully installed`가 보이면 성공입니다.

> **⚠️ "스크립트 실행 보안" 오류**가 뜨면:
> ```powershell
> Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
> ```
> 한 번 실행한 뒤 `Y` 누르고, 위의 ④ 명령을 다시 시도하세요.

이제 [4.4 KRX 계정 만들기](#44-krx-계정-만들고-env-채우기-한국-주식용)로 넘어가세요.

---

### 4.4 KRX 계정 만들고 .env 채우기 (한국 주식용)

KOSPI / KOSDAQ 종목 검색을 쓰려면 한국거래소(KRX) 무료 계정이 필요합니다.
미국 주식·코인만 쓰실 거면 이 단계는 **건너뛰어도 됩니다**.

#### ① KRX 회원가입

- <https://data.krx.co.kr/> 접속
- 우측 상단 **"회원가입"** 클릭
- 본인 인증 → ID·비밀번호 정하기 (이미 있으면 그대로 사용)

#### ② `.env` 파일 만들기

프로젝트 폴더 안에 `.env.example`이라는 예시 파일이 들어있습니다.
이걸 **복사해서 이름을 `.env`로 바꿔주세요**.

**macOS 터미널**:
```bash
cp .env.example .env
```

**Windows PowerShell**:
```powershell
Copy-Item .env.example .env
```

#### ③ `.env` 파일 편집

생긴 `.env` 파일을 텍스트 에디터로 엽니다.

- **macOS**: 터미널에서 `open -a "TextEdit" .env`
- **Windows**: 파일 탐색기에서 `.env` 파일을 마우스 우클릭 → "연결 프로그램 → 메모장"

파일 안에 다음 두 줄을 본인 KRX 계정 정보로 채웁니다:

```env
KRX_ID=내아이디
KRX_PW=내비밀번호
```

저장 후 닫으세요. 다른 항목(BINANCE_*, UPBIT_*)은 그대로 비워두면 됩니다.

> 🔒 `.env` 파일은 본인 컴퓨터에만 남고 다른 사람에게 공유되지 않습니다.

---

### 4.5 매번 실행하는 방법

설치가 끝났으니 이제부터는 **이 단계만 반복**하면 됩니다.

**macOS 터미널**:
```bash
cd ~/Desktop/Regret-monger-simulator
.venv/bin/python main.py
```

**Windows PowerShell**:
```powershell
cd $HOME\Desktop\Regret-monger-simulator
.\.venv\Scripts\python main.py
```

앱 창이 뜨면 성공입니다. 종료할 때는 그냥 **창의 X 버튼**을 누르거나 터미널에서 `Ctrl+C`.

> **더 쉽게 실행하기**: 위의 두 줄을 메모장에 적어두고 매번 복사해서 쓰세요.
> 또는 다음 챕터의 "바로가기 만들기" 팁을 참고하세요.

---

### 4.6 자주 막히는 곳 / 문제 해결

**Q. `python3: command not found` 또는 `'python'은 내부 또는 외부 명령이 아닙니다`**
- Python이 설치 안 됐거나 PATH에 등록 안 된 경우입니다.
- macOS: 4.2 ②번을 다시 진행해주세요.
- Windows: 4.3 ②번에서 **"Add python.exe to PATH"** 체크 누락이 흔한 원인입니다.
  Python 인스톨러를 다시 실행 → "Modify" → 체크박스 켜고 재설치.

**Q. `pip install` 도중에 빨간 에러가 잔뜩 떠요**
- 한두 줄짜리 경고는 무시해도 됩니다 (마지막에 `Successfully installed`가 보이면 OK).
- 진짜 실패라면 인터넷 연결을 확인하고 명령을 다시 한 번 실행해주세요.

**Q. "차트 불러오기"를 눌렀는데 "에러"가 떠요**
- 티커를 잘못 입력한 경우가 가장 흔합니다.
- 마켓을 바꿨는데 옛 티커가 그대로 남아있거나, 비공개 종목이거나.
- 자동완성 드롭다운에서 골라주세요.

**Q. KOSPI 자동완성이 안 돼요**
- `.env` 파일에 KRX_ID/KRX_PW를 안 넣었거나, 잘못 입력했을 가능성이 큽니다.
- 4.4를 다시 확인해주세요. KRX 사이트에서 직접 로그인 테스트해보면 좋습니다.

**Q. 앱이 너무 느려요 / 프리징돼요**
- 첫 실행 시 종목 디렉터리(KOSPI 약 5초, NASDAQ 1초)를 받느라 잠깐 느립니다.
- 두 번째부터는 캐시되어 즉시 동작합니다.

**Q. 이미 설치한 적이 있는데 새 압축파일을 다시 받았어요**
- 옛 폴더는 지우고, 새 폴더에서 처음부터 (4.2 ③번부터) 다시 설치하시면 됩니다.

---

## 5. 시간대·정수매매 등 주의사항

- **모든 일자는 tz-naive `date`로 정규화**된다.
  - 한국 주식: KST 일자
  - 미국 주식: ET 일자
  - 크립토: **UTC 일자** (24시간 시장이라 UTC 자정 기준 캔들)
- **정수매매**: 한국·미국 주식은 0.5주 같은 분수 매수 불가. 금액 입력 시 자동으로 floor 처리.
- **수익률은 `Adj Close` 기반**: 배당·액면분할이 자동 보정됨. 매수단가 표시는 raw OHLC.
- **휴장일 입력**: 차트 클릭은 캔들에만 스냅되므로 자연스럽게 회피. 수동 입력 시 직전/직후 선택 다이얼로그.
- **첫 실행 시 시간**: 종목 디렉터리(KRX 약 5초, Binance/NASDAQ 약 1초)를 백그라운드로 받아온다. 7일 캐시.
- **OHLC 캐시**: 같은 (마켓, 티커, 기간) 조회는 두 번째부터 즉시 (parquet, `~/.regret/cache/`).
  데이터 어댑터를 수정한 뒤 캐시가 이상하게 동작하면 디렉터리째 삭제.

---

## 6. v1 범위 외 (TODO)

- 세금 계산 (양도세 등)
- 다중 통화 환산 비교 (KRW ↔ USD 환율)
- 복수 포트폴리오 동시 비교
- 실시간 가격 스트리밍 (현재는 스냅샷)
- 매도 시뮬레이션 (현재는 보유 가정)
